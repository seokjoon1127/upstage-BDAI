# 이 파일을 .env 로 복사한 뒤 값을 채운다. .env 는 절대 커밋하지 않는다.
# Timely 에 올릴 때는 이 이름들을 [설정 > 환경변수] 에 등록한다.

# 서울 열린데이터광장 인증키 (가입 즉시 발급)
SEOUL_API_KEY=

# 기업마당 지원사업정보 API 키 (승인 대기 가능 — 없으면 파일데이터로 대체)
BIZINFO_API_KEY=

# 한국부동산원 임대동향조사는 파일 다운로드라 키가 필요 없다.
