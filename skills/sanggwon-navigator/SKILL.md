---
name: sanggwon-navigator
description: >
  서울시 공공 상권 데이터로 특정 지역·업종의 창업 적합도를 진단하고, 한국부동산원 임대료·공실률로
  진입비용을 함께 산출한 뒤, 지금 신청 가능한 소상공인·창업 지원제도를 조건별로 매칭해 리포트 한 장으로 만든다.
  사용자가 서울의 특정 동네·상권에 가게를 내는 것을 물을 때("성수동에 카페 차려도 될까?",
  "연남동 상권 어때?", "여기 창업해도 되나", "월세 대비 남을까"),
  후보 지역이나 업종을 비교할 때, 창업 지원금·정책자금·소상공인 지원제도를 물을 때 사용한다.
  업종만 주어진 입지 탐색("카페 하기 좋은 동네 어디야?")과 지역만 주어진 업종 탐색
  ("성수동에서 뭐가 잘 돼?")에도 사용한다. 이미 운영 중인 점포의 상권 점검과 경영안정 지원제도
  확인에도 사용한다.
  서울 이외 지역이나 해외 상권의 진단, 창업 아이템 브레인스토밍, 사업계획서 작성,
  개별 매물·공실 위치 찾기에는 사용하지 않는다.
---

# 상권 내비게이터

서울 골목상권 1,650개의 매출·유동인구·개폐업 데이터로 **상권력**을,
한국부동산원 상권 59개의 임대료·공실률로 **진입비용**을 각각 판정하고,
기업마당 지원사업 1,500건에서 오늘 신청 가능한 제도를 조건별로 갈라 보여준다.

원칙 두 가지:

- **조건은 필터가 아니라 분기다.** 나이·성별·창업경력을 묻지 않고 조건별로 전부 보여준다.
- **등급을 하나로 합치지 않는다.** "매출이 좋아서 B"인지 "월세가 싸서 B"인지 구분되어야 한다.

---

## Workflow

### Step 0 — 입력 정규화

발화에서 **지역**과 **업종**을 뽑는다.

- 업종은 `reference/service_codes.yaml` 로 매핑한다. 애매한 업종(피자·마라탕·애견카페 등)은
  **기본값으로 진행하되 대안을 리포트에 표기**한다. 되묻지 않는다.
- 지역은 상권명 → 행정동 → 자치구 순으로 좁혀 찾는다. 서울 행정동의 64%가 `성수1가1동` 처럼
  쪼개져 있어 `성수동` 과 직접 매칭되지 않으므로, 스크립트가 어간으로 다시 찾는다.

**질문은 이 단계에서 최대 1회.** 이후에는 묻지 않는다.

### Step 1 — 모드 판별

| 지역 | 업종 | 모드 | 실행 |
|---|---|---|---|
| ✓ | ✓ | **진단** | `make_report.py --region <지역> --biz <업종>` |
| ✗ | ✓ | **입지 탐색** | `analyze.py --biz <업종> --top 10` |
| ✓ | ✗ | **업종 탐색** | 해당 상권에서 업종별로 반복 후 상위 제시 |
| ✗ | ✗ | **개요 브리핑** | 서울 상권 트렌드 요약 + 좁히기 선택지 제시 |

슬롯이 비었다고 되묻지 말고 **다른 모드로 해석한다.**

### Step 2 — 실행

```bash
cd <스킬 폴더>
pip install pandas pyarrow pyyaml     # 없을 때만
python scripts/make_report.py --region 성수동 --biz 카페
```

캐시가 없거나 낡았으면 데이터를 자동으로 받는다(약 2분). 사람이 개입하지 않는다.
상권을 직접 고르려면 `--trdar 성수동카페거리`, 네트워크가 막히면 `--skip-policy`.

산출물: `output/<지역>_<업종>_<분기>.md`

#### 인증키 — 사용자에게 묻지 않는다

스크립트는 키를 **환경변수에서 직접 읽는다.** 필요한 이름은 셋이다.

| 환경변수 | 용도 |
|---|---|
| `SEOUL_API_KEY` | 서울 상권 데이터 |
| `REB_API_KEY` | 부동산원 임대료·공실률·권리금 |
| `BIZINFO_API_KEY` | 기업마당 지원제도 |

- **키가 없다는 에러가 나면 사용자에게 키를 달라고 하지 않는다.**
  대신 *"[설정 > 환경변수]에 `SEOUL_API_KEY` 를 등록해 주세요"* 라고 안내한다.
- **키를 채팅창에 출력하거나 되묻지 않는다.** 대화 기록에 남으면 유출이다.
- 키가 없어도 되는 일은 계속한다. 예를 들어 `--skip-policy` 로 상권 진단만 낼 수 있다.

### Step 3 — 결과 해석 규칙

리포트를 그대로 붙여넣지 말고 **한눈에 표와 처방을 먼저 말한 뒤** 근거를 붙인다.

| 표시 | 뜻 | 전달 방법 |
|---|---|---|
| **표본부족** | 나쁜 상권이 아니라 **점포가 적어 평균을 낼 수 없음** | "판정 불가"로 말한다 |
| **여러 자치구에 걸침** | 같은 이름 동이 여러 구에 있음 (신사동·도곡동) | 어느 구 기준인지 밝히고 좁히는 법을 알려준다 |
| **진입비용** | 상권력과 **별개 축**이다 | 절대 합산해 말하지 않는다 |
| **공실률 높음** | 임차인에겐 **협상 여지** | 나쁜 신호로 말하지 않는다 |
| **빈 조회** | 0이 아니라 **지표 없음** | 0원·최악으로 해석하지 않는다 |

모든 수치에 **기준 분기**를 함께 말한다.

#### ⚠️ 지표를 거꾸로 읽지 않는다

지표 표의 판정 칸에는 **유리 / 보통 / 불리**가 이미 적혀 있다. **그 판정을 그대로 쓴다.**
백분위 숫자만 보고 임의로 해석하지 마라. 특히 아래 둘이 자주 뒤집힌다.

| 지표 | 잘못된 해석 | 올바른 해석 |
|---|---|---|
| **경쟁 밀도 ↓좋음** | "상위 13% → 경쟁자가 적다" ❌ | 판정이 **불리**면 **경쟁이 심하다** |
| **생존 신호** | "−3.0%p → 개업이 더 많다" ❌ | **음수면 폐업이 개업보다 많다** |

경쟁 밀도는 값이 낮을수록 좋은 지표라 백분위를 뒤집어 저장한다.
생존 신호는 `개업률 − 폐업률` 이므로 **부호가 곧 방향**이다.

### Step 4 — 넘기기 전 확인

- [ ] 등급과 함께 **왜 그 등급인지**(기여도 상위 지표 2개)를 말했는가
- [ ] **처방**(노려라/자금력 싸움/저리스크 실험/피하라)을 말했는가
- [ ] 후보 상권이 여러 개면 어느 곳 기준인지 밝혔는가
- [ ] 마감 임박(D-14 이내) 지원제도를 따로 짚었는가
- [ ] 데이터 기준 시점과 출처를 밝혔는가

---

## 묻지 말고 듣는다

사용자가 **자발적으로 말하면** 반영한다. 먼저 묻지는 않는다.

```
"성수동에 카페"           → 조건별 분기 전부 제시
"35살인데 성수동에 카페"   → 청년 분기를 위로 올려 강조 + 나머지도 그대로 제시
```

나이·성별·창업경력을 물으면 그게 곧 **사람 개입**이고, 40세인 사람이
"39세 이하면 3건 더 있다"를 못 보게 된다.

---

## 판단 기준은 어디에 있나

숫자와 근거는 전부 파일에 있다. 코드에 하드코딩하지 않는다.

| 무엇 | 어디 |
|---|---|
| 가중치·컷오프·강등 규칙·부담률 구간 | `config/defaults.yaml` |
| 그 숫자를 왜 그렇게 정했는지 | `reference/scoring_rules.md` |
| 업종 매핑과 애매 업종 판단 | `reference/service_codes.yaml` |
| 정책 조건 분기 키워드 | `reference/policy_keywords.yaml` |
| 서울시 상권 ↔ 부동산원 상권 대응 | `reference/rent_districts.yaml` |

---

## 입력 / 출력

| 인자 | 필수 | 예 |
|---|---|---|
| `--region` | ✓ | 성수동 · 연남동 · 성동구 · "강남 신사동" |
| `--biz` | ✓ | 카페 · 한식음식점 · 치킨집 · 피자 |
| `--trdar` | | 성수동카페거리 (기본값: 매출 1위 상권) |
| `--skip-policy` | | 정책 조회 생략 |

출력은 마크다운 리포트 한 장 — 한눈에 표 · 등급 근거 · 진입비용 · 후보 상권 비교 ·
조건별 지원제도 · 출처와 한계.

---

## 하지 말 것

- **서울 외 지역의 상권 등급을 내지 않는다.** 데이터가 없다. 지원제도만 제공한다.
- **개별 매물이나 공실 위치를 찾아주지 않는다.** 공공데이터에 없다. 상권 단위 공실률만 제공한다.
- **지원제도의 최종 자격을 단정하지 않는다.** 공고 키워드로 분류한 것이므로
  "공고문에서 자격요건을 직접 확인하라"를 반드시 덧붙인다.
- **나이·성별·창업경력을 묻지 않는다.**
- **`--refresh` 를 습관적으로 쓰지 않는다.** 서울 API에 일일 호출 한도가 있다.

---

## 쓰는 도구

| 도구 | 어디에 |
|---|---|
| **Upstage Document Parse** | 서울시 상가임대차 실태조사 스캔 PDF(78쪽, 텍스트 0자)에서 140개 상권 임대료 표 추출 |
| **Upstage Solar** (`solar-pro2`) | Document Parse 가 뽑은 표는 5블록 병합이라 셀 정렬이 깨진다. 이를 JSON 으로 정형화 |

```bash
python scripts/parse_lease_pdf.py --pdf data/raw/seoul_lease_2022.pdf --page 22
```

원본이 연 1회 갱신이라 매 실행마다 호출하지 않는다.
결과는 `reference/seoul_lease_districts.csv` 로 저장해 두고 쓰며,
새 보고서가 나오면 스크립트를 다시 돌린다. `UPSTAGE_API_KEY` 가 필요하다.

---

## 데이터 갱신

상권·임대료 데이터는 분기마다 바뀐다. 캐시에 기준 분기가 스탬프로 박혀 있고,
최신 분기와 벌어지면 자동으로 다시 받는다. 지원제도는 마감일이 매일 바뀌므로 캐시하지 않는다.

```bash
python scripts/fetch_data.py --status    # 상권 캐시 상태
python scripts/rent.py --status          # 임대료 캐시 상태
python scripts/fetch_data.py --refresh   # 분기 갱신 (스케줄 자동화용)
```
